version: '3.8'

services:
  skillpath:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: skillpath_app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - SKIP_DB=true
    restart: unless-stopped
